import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#082032",
    text: "#F7F7F7",
    buttons: "#4FBDBA",
    hoverbg: "#395B64",
  },
});

export default theme;
