import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#082032", // background
    secondary: "#395B64", // hover
    text: "#F7F7F7",
    buttons: "#4FBDBA",
  },
});

export default theme;
